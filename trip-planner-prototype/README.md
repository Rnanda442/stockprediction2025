# Carry prototype

Static product prototype for a group travel planning app. It intentionally uses
fake data and does not accept payments, connect bank accounts, or issue cards.

## Run locally

From the repository root:

```powershell
python -m http.server 4173 --directory trip-planner-prototype
```

Then open `http://localhost:4173`.
